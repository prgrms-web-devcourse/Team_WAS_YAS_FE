import { Spinner } from '@/components';

export default {
  title: 'Components/Atoms/Spinner',
  component: Spinner,
};

export const Default = (): JSX.Element => {
  return (
    <>
      <Spinner />
    </>
  );
};
