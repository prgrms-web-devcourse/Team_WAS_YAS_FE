import { UserEditPage } from '@/pages';

export default {
  title: 'Components/Pages/UserEditPage',
  component: UserEditPage,
};

export const Default = (): JSX.Element => {
  return <UserEditPage />;
};
